export type ExperienceEntry = {
  company: string;
  role: string;
  dates: string;
};

// TODO: replace with real roles — company, role, dates only, no paragraphs.
export const experience: ExperienceEntry[] = [
  { company: "TODO Company", role: "TODO Role", dates: "TODO – TODO" },
];
